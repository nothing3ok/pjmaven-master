本版本完成两个优化
1 每次在响应客户端前，通过httpresponse 的setentity方法这是相应的具体资源文件时都要在调用两个
添加说明方法，相应正文长度和两个响应头：content-type，content-length因此我们可以将这两个响应头
的设置工作迁移到setentity方法中完成，这样每次只需要设置相应正文，而这两个响应头就同时添加了

2 httpcontext 中现在支持的资源类型与content-type 只有几个 而tomcat 对此已经整理了所有的类型
共1000 多个，我们直接使用它提供的文件在初始化，使得我们也能支持所有的类型。